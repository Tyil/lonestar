#! /usr/bin/env bash

fetch_src()
{
	version=$1
	dl_url="http://rakudo.org/downloads/star/rakudo-star-$version.tar.gz"
	tmp=$(mktemp -d)

	curl "$dl_url" > "$tmp/$version.tar.gz"

	mkdir -p "$RAKUDO_ROOT"
	tar xzf "$tmp/$version.tar.gz" -C "$RAKUDO_ROOT/"
	rm -r "$tmp"
}

export -f fetch_src
